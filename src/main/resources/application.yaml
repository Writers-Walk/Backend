spring:
  application:
    name: bookapp

#DB
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:bookdb;DB_CLOSE_DELAY=-1
    username: sa
    password: 1234
  h2:
    console:
      enabled: true
      path: /h2-console

#JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#Server
server:
  port: 8080
  servlet:
    session:
      timeout: 60m
logging:
  level:
    org:
      springframework:
        web: DEBUG
springdoc:
  swagger-ui:
    path: /swagger
